import { type StageObjectData } from "@/types/stage-object"

type Props = {
  id: string
  selectedObject: StageObjectData
}

const TextColorPicker = ({ id, selectedObject }: Props) => {
  return <></>
}

export default TextColorPicker
